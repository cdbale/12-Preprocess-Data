---
title: "Preprocess Data"
subtitle: "MCB Consulting Group (MKTG 411)"
output: 
  ioslides_presentation:
    widescreen: true
    css: style.css
---

## Key Concepts | Prepare the Data for the Story

- **Preprocessing:** data wrangling for models, including transformations and feature engineering.
- **Dummy coding:** express categorical levels as binary indicator variables.
- **Reference levels:** interpret each indicator relative to its omitted baseline.
- **Consistent predictions:** apply the training recipe to testing data and counterfactuals, then report results in meaningful units.

Today we develop the **preprocess** stage introduced in lecture 07 and connect it to the full modeling workflow.

## Marketing Analytics Process

<center>
![](Figures/process_model.png){width=900px}
</center>

## Inferential Modeling Workflow

<center>
![](Figures/workflow-inference_preprocess.png){width=900px}
</center>

---

![](Figures/hex_recipes.png){width=500px}

## Preprocess Data

So far we have been trying to slowly build intuition about inferential models. Now we want to talk about the data itself. In particular, we often need to **preprocess the data** so it is appropriate for the model to use. This may involve creating new variables as well. This is also known as **feature engineering**. Again, think of *preprocessing* as *data wrangling for models*.

We've technically already done some preprocessing: data splitting.

```{r message=FALSE}
# Load packages.
library(tidyverse)
library(tidymodels)

# Import soup data.
soup_data <- read_csv("soup_data.csv")

# Split data.
soup_split <- initial_time_split(soup_data, prop = 0.90)
```

---

Preprocessing data is made easier using the recipes package where common preprocessing tasks have been broken down into steps, beginning with a `recipe()` where we identify the model outcome and explanatory variables (a.k.a., predictors) using *formula* notation.

```{r}
soup_recipe <- training(soup_split) |> 
  recipe(Sales ~ Any_Disp_Spend + Any_Price_Decr_Spend + Sub_Category)
```
---

```{r}
soup_recipe
```


---

<center>
![](Figures/monsters_recipes.png){width=900px}
</center>

## Here's How We Think About Recipes...

- The **Data** is our *pantry* - where we find all of the ingredients
- We are making a dish for our guest of honor - the **Model**
- The model expects an outcome variable and explanatory variables prepared in specific ways
- We define a *Recipe* that outlines how our ingredients (outcome and explanatory variables) must be prepared for our model

## What Will We Do Today?

- Discuss some common pre-processing (recipe) steps
- Practice creating a recipe
- See the impact that pre-processing can have on model results

## Right-Skewed Continuous Data

A common problem with data is having **right-skewed continuous data**, something to identify when we're doing *exploratory data analysis*.

Why is right-skewed continuous data common?

```{r eval=FALSE}
# Visualize Sales.
soup_data |> 
  ggplot(aes(x = Sales)) +
  geom_histogram()
```

---

```{r echo=FALSE}
# Visualize Sales.
soup_data |> 
  ggplot(aes(x = Sales)) +
  geom_histogram()
```

---

It's not just `Sales`.

```{r eval=FALSE}
# Visualize all continuous variables.
soup_data |> 
  pivot_longer(
    c(Sales, Any_Disp_Spend, Any_Price_Decr_Spend),
    names_to = "var_names",
    values_to = "cont_values"
  ) |> 
  ggplot(aes(x = cont_values)) +
  geom_histogram() + 
  facet_wrap(~ var_names)
```

---

```{r echo=FALSE}
# Visualize all continuous variables.
soup_data |> 
  pivot_longer(
    c(Sales, Any_Disp_Spend, Any_Price_Decr_Spend),
    names_to = "var_names",
    values_to = "cont_values"
  ) |> 
  ggplot(aes(x = cont_values)) +
  geom_histogram() + 
  facet_wrap(~ var_names)
```

## Pause! Why did the plot report that values were removed?

## Let's Check for Missing Values

```{r}
soup_data |> 
  filter(is.na(Sales) | is.na(Any_Disp_Spend) | is.na(Any_Price_Decr_Spend)) |> 
  select(Sales, Any_Disp_Spend, Any_Price_Decr_Spend)
```

## Plot without Missing Values

```{r echo=FALSE}
# Visualize all continuous variables.
soup_data |> 
  filter(!is.na(Sales), !is.na(Any_Disp_Spend), !is.na(Any_Price_Decr_Spend)) |>
  pivot_longer(
    c(Sales, Any_Disp_Spend, Any_Price_Decr_Spend),
    names_to = "var_names",
    values_to = "cont_values"
  ) |> 
  ggplot(aes(x = cont_values)) +
  geom_histogram() + 
  facet_wrap(~ var_names)
```

---

Recall lecture 07's distinction between the outcome and statistical error $\epsilon$. Normal-error regression concerns errors **conditional on the predictors**; it does not require the outcome or predictors themselves to have normal histograms. A `log()` transform can help model skewed data, nonlinear relationships, or changing error spread. Evaluate whether it improves the model rather than assuming every skewed variable needs transformation.

## Explore a Log Transformation

- Why a `log()` transform? What is a logarithm?
- What is the *inverse* of a logarithm?

```{r eval=FALSE}
# Use the log() transform.
soup_data |> 
  pivot_longer(
    c(Sales, Any_Disp_Spend, Any_Price_Decr_Spend),
    names_to = "var_names",
    values_to = "cont_values"
  ) |> 
  mutate(log_cont_values = log(cont_values + 1)) |> 
  ggplot(aes(x = log_cont_values)) +
  geom_histogram() + 
  facet_wrap(~ var_names, scales = "free")
```

---

```{r echo=FALSE}
# Use the log() transform.
soup_data |> 
  pivot_longer(
    c(Sales, Any_Disp_Spend, Any_Price_Decr_Spend),
    names_to = "var_names",
    values_to = "cont_values"
  ) |> 
  mutate(log_cont_values = log(cont_values + 1)) |> 
  ggplot(aes(x = log_cont_values)) +
  geom_histogram() + 
  facet_wrap(~ var_names, scales = "free")
```

---

We add this step to our recipe using `step_log()`. We can reference all continuous variables in the model with `all_numeric()`. The argument `offset = 1` says to add 1 before using `log()` so we can avoid `log(0)`.

```{r}
soup_recipe <- training(soup_split) |> 
  recipe(Sales ~ Any_Disp_Spend + Any_Price_Decr_Spend + Sub_Category) |> 
  step_log(all_numeric(), offset = 1)

soup_recipe
```

## Discrete Data

Lecture 07 used soup sub-categories to introduce dummy coding. These are **categorical labels**, not equally spaced numbers. How many levels occur in the full data, and how many indicators will an intercept-based regression need?

```{r}
soup_data |> 
  count(Sub_Category)
```

## Dummy Coding | Two Soup Categories

First consider only `CONDENSED SOUP` and `RAMEN`, as in lecture 07. We can create a binary column for each label:

| `Sub_Category` | `condensed_soup` | `ramen` |
|---|---:|---:|
| CONDENSED SOUP | 1 | 0 |
| RAMEN | 0 | 1 |

With an intercept, keep only one indicator. If condensed soup is the **reference level**, `ramen = 0` represents condensed soup and `ramen = 1` represents ramen.

The ramen coefficient is its expected difference from condensed soup, holding the other predictors fixed. Dropping an indicator does not drop those observations.

## Dummy Coding | More Levels, More Indicators

Now add `DRY SOUP`. Using condensed soup as the baseline gives:

| `Sub_Category` | `ramen` | `dry_soup` |
|---|---:|---:|
| CONDENSED SOUP | 0 | 0 |
| RAMEN | 1 | 0 |
| DRY SOUP | 0 | 1 |

- With an intercept, a categorical predictor with $k$ levels uses $k - 1$ indicators.
- All $k$ indicators sum to 1, duplicating the intercept: an extreme case of the multicollinearity from lecture 10.
- Each categorical predictor has its own baseline; this table illustrates three categories, not the full data's level count.

## Put Dummy Coding into the Recipe

We add this step to our recipe using `step_dummy()`. We could reference all discrete variables in the model with `all_nominal()`.

The default creates $k - 1$ indicators. Check the reference level before interpreting coefficients; do not assume that the illustrative baseline above was selected automatically.

```{r}
soup_recipe <- training(soup_split) |> 
  recipe(Sales ~ Any_Disp_Spend + Any_Price_Decr_Spend + Sub_Category) |> 
  step_log(all_numeric(), offset = 1) |> 
  step_dummy(Sub_Category)

soup_recipe
```

## Prepare a Recipe

We can build the recipe all at once. Once we're ready, we can `prep()` the recipe using the training data. This learns any data-dependent settings, including category levels, from training data only. Reuse that prepared recipe with `bake()` for testing data and scenarios; do not prepare a separate recipe on each data set.

```{r}
soup_recipe <- training(soup_split) |> 
  recipe(Sales ~ Any_Disp_Spend + Any_Price_Decr_Spend + Sub_Category) |>
  step_log(all_numeric(), offset = 1) |> 
  step_dummy(Sub_Category) |> 
  prep()
```

## Apply a Recipe

Following the recipe analogy, we apply a prepared recipe to data using `bake()`.

```{r}
# Apply the recipe to the training data.
baked_soup_training <- soup_recipe |>
  bake(training(soup_split))
```

---

```{r}
baked_soup_training
```

## Fit with Preprocessed Training Data

Let's compare models with and without the preprocessed data.

```{r}
# Fit a model without preprocessed data.
fit_raw <- linear_reg() |> 
  set_engine("lm") |> 
  fit(
    Sales ~ Any_Disp_Spend + Any_Price_Decr_Spend + Sub_Category, 
    data = training(soup_split)
  )

# Fit a model with preprocessed data.
fit_baked <- linear_reg() |> 
  set_engine("lm") |> 
  fit(
    Sales ~ ., 
    data = baked_soup_training
  )
```

## Evaluate Parameter Estimates

If we want to use it, there is a horizontal version of the error bars plot.

```{r eval=FALSE}
# Compare parameter estimates.
tidy(fit_baked, conf.int = TRUE) |> 
  ggplot(aes(y = term)) + 
  geom_point(aes(x = estimate)) + 
  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), orientation = 'y') +
  geom_vline(xintercept = 0, color = "red")
```

## Error Bar plot for 'Raw' Data

```{r echo=FALSE}
# Compare parameter estimates.
tidy(fit_raw, conf.int = TRUE) |> 
  ggplot(aes(y = term)) + 
  geom_point(aes(x = estimate)) + 
  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), orientation = 'y') +
  geom_vline(xintercept = 0, color = "red")
```

## Error Bar plot for Pre-processed Data

```{r echo=FALSE}
# Compare parameter estimates.
tidy(fit_baked, conf.int = TRUE) |> 
  ggplot(aes(y = term)) + 
  geom_point(aes(x = estimate)) + 
  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), orientation = 'y') +
  geom_vline(xintercept = 0, color = "red")
```

---

$$\log(\text{Sales} + 1) = 5.42 + 0.12 \times \log(\text{Any_Disp_Spend} + 1) + \\
... + .18 \times \text{Sub_Category_RTS.SOUP} + ...$$

The recipe transforms `Sales` and the numeric spending variables with `log(value + 1)`; the sub-category indicators remain 0/1.

## Interpreting Transformed Relationships

How does the log transform change parameter interpretations?

- *y* and $x_1$: A **one unit increase** in $x_1$ leads to a **$\beta_1$ unit change** in *y* (relative to the baseline level of $x_1$), holding all other variables fixed.
- log(*y*) and $x_1$: A **one unit increase** in $x_1$ leads to a **100 × $\beta_1$% change** in *y* (relative to the baseline level of $x_1$), holding all other variables fixed.
- *y* and log($x_1$): A **1% increase** in $x_1$ leads to a **$\beta_1$/100 unit change** in *y* (relative to the baseline level of $x_1$), holding all other variables fixed.
- log(*y*) and log($x_1$): A **1% increase** in $x_1$ leads to a **$\beta_1$% change** in *y* (relative to the baseline level of $x_1$), holding all other variables fixed.

These percentage rules are approximations for unshifted logs. In our recipe, they concern `Sales + 1` and `spend + 1`. For budget decisions, compare scenario predictions after returning to sales dollars.

## Your Turn!

We need to compute the RMSE of our two models using the testing data. Make sure to apply all necessary pre-processing steps to the testing data before applying the model that was trained on the pre-processed training data.

```{r echo = FALSE}
# Apply the recipe to the testing data.
baked_soup_testing <- soup_recipe |>
  bake(testing(soup_split))

bind_rows(
  # Compute RMSE without preprocessed testing data.
  predict(fit_raw, new_data = testing(soup_split)) |>
    bind_cols(testing(soup_split)) |>
    rmse(truth = Sales, estimate = .pred),
  # Compute RMSE with preprocessed testing data.
  predict(fit_baked, new_data = baked_soup_testing) |>
    bind_cols(baked_soup_testing) |>
    rmse(truth = Sales, estimate = .pred)
)
```

## Solution

```{r eval = FALSE}
# Apply the recipe to the testing data.
# THIS is why recipes are so helpful - they allow you to save 
# your pre-processing steps and apply them across different data frames
baked_soup_testing <- soup_recipe |>
  bake(testing(soup_split))

bind_rows(
  # Compute RMSE without preprocessed testing data.
  predict(fit_raw, new_data = testing(soup_split)) |>
    bind_cols(testing(soup_split)) |>
    rmse(truth = Sales, estimate = .pred),
  # Compute RMSE with preprocessed testing data.
  predict(fit_baked, new_data = baked_soup_testing) |>
    bind_cols(baked_soup_testing) |>
    rmse(truth = Sales, estimate = .pred)
)
```

## Wait...Were the Previous RMSE values Comparable?

## Wait...Were the Previous RMSE values Comparable?

No! One error measure was computed on the `log(Sales + 1)` scale. To compare models, predictions and observed outcomes must use the same scale: apply `log(.pred + 1)` to raw-scale predictions, or `exp(.pred) - 1` to transformed-scale predictions. The added 1 in our recipe must be reversed too.

## The Model Improved...But not THAT Much

Evaluation on raw scale.

```{r}
bind_rows(
  # Compute RMSE without preprocessed testing data.
  predict(fit_raw, new_data = testing(soup_split)) |>
    bind_cols(testing(soup_split)) |>
    rmse(truth = Sales, estimate = .pred),
  # Compute RMSE after transforming predictions from preprocessed testing data
  predict(fit_baked, new_data = baked_soup_testing) |>
    bind_cols(testing(soup_split)) |>
    mutate(.pred = exp(.pred) - 1) |>
    rmse(truth = Sales, estimate = .pred)
)
```

## The Model Improved...But not THAT Much

Evaluation on log scale.

```{r}
bind_rows(
  # Compute RMSE after transforming predictions from raw testing data.
  predict(fit_raw, new_data = testing(soup_split)) |>
    bind_cols(baked_soup_testing) |>
    mutate(.pred = log(.pred + 1)) |>
    rmse(truth = Sales, estimate = .pred),
  # Compute RMSE for predictions from preprocessed testing data
  predict(fit_baked, new_data = baked_soup_testing) |>
    bind_cols(baked_soup_testing) |>
    rmse(truth = Sales, estimate = .pred)
)
```

## Apply a Recipe for Prediction

Having all of the preprocessing steps saved as a recipe means we can easily apply it to `new_data` when using `predict()` to evaluate counterfactuals. Construct the counterfactual data in the same form as the raw data so that it will work with our recipe.

This returns to lecture 07's managerial question: how should Kroger allocate promotional spending? Changing the input values defines the scenario; preprocessing expresses those inputs in the form our fitted model expects.

## Recall the Format of the Raw Data

```{r}
soup_data |>
  select(Any_Disp_Spend, Any_Price_Decr_Spend, Sub_Category, Sales)
```

## Construct Counterfactuals

Allocate $10,000 spend between display and price decreases for each sub-category.

```{r}
# Counterfactual scenarios.
scenarios <- tibble(
  # compute display spend numbers, repeat them 5 times, once for each category
  Any_Disp_Spend = seq(from = 0, to = 10000, by = 2000) |> rep(5),
  # compute price decrease spend numbers, repeat them 5 times, once for each category
  Any_Price_Decr_Spend = seq(from = 10000, to = 0, by = -2000) |> rep(5),
  # character vector of subcategories, repeated six times six we have six counterfactual scenarios
  # in each sub-category
  Sub_Category = unique(soup_data$Sub_Category) |> rep(6) |> sort(),
  Sales = 1
)
```

---

What's different about this `new_data`? How can we interpret each of the rows?

```{r}
scenarios
```

---

Apply the recipe and remove the outcome variable before using the new data for `predict()`.

```{r}
# Apply the recipe to the training data.
baked_scenarios <- soup_recipe |>
  bake(scenarios) |> 
  select(-Sales)

baked_scenarios
```

---

Generate point predictions and predictive intervals.

```{r}
# Predict and bind on prediction intervals.
bind_cols(
  predict(fit_baked, new_data = baked_scenarios),
  predict(fit_baked, new_data = baked_scenarios, type = "pred_int"),
  baked_scenarios
) |> 
  arrange(desc(.pred))
```

## Return the Scenarios to the Managerial Decision

The model above predicts `log(Sales + 1)`. Before communicating the scenarios to Kroger:

- Transform point predictions and prediction-interval endpoints with `exp(value) - 1` to return to sales dollars.
- Compare feasible budget allocations on that same scale, with their uncertainty and sub-category baselines in view.
- Explain that simply reversing a log prediction does not generally estimate **mean** sales; under normal log-scale errors it estimates the conditional median.
- Treat the recommendation as conditional on the model's story. Neither preprocessing nor good predictive fit proves what a spending intervention will cause.

Which additional evidence would make the budget recommendation more convincing?

## Other Preprocessing Steps

We can manually wrangle everything that a step function can do, but step functions can be a lot easier to use. Beyond `step_log()` and `step_dummy()`, for example:

- `step_corr()` to check for multicollinearity and drop explanatory variables.
- `step_normalize()` to subtract the mean and divide by the variance.
- `step_discretize()` to make a continuous variable discrete.
- `step_num2factor()` to quickly turn a numeric variable into a factor.

We also have additional helper functions like `all_predictors()`.

## Final Note: Important!

REMEMBER THESE POINTS:

Preprocessing occurs in three places in our analysis.

AFTER you do the train-test split,

1.  on the training data, prior to training your model(s).
2.  on the testing data, prior to generating predictions to evaluate model fit.
3.  on the counterfactuals, prior to generating counterfactual predictions

- Make sure you reverse any pre-processing steps necessary for your predictions to be on the original scale.

## Live Coding

Our old client, **iFood**, has requested an updated model for total sales. Now that we have knowledge of various pre-processing skills, let's build and assess a new model for them.

```{r}

```

## Wrapping Up

*Summary*

- Walked through preprocessing data using recipes.
- Demonstrated the log transform and dummy coding.
- Completed the story-to-decision workflow by applying one training recipe to observed and hypothetical data.

*Next Time*

- What if the outcome isn't continuous?

*Supplementary Material*

- *Tidy Modeling with R* Chapter 8

*Artwork by @allison_horst*

## Exercise 11

Return again to `soup_data` and your models used in Exercises 9 and 10.

1. Check for right-skewed continuous variables.
2. Prepare a recipe using the necessary steps.
3. Fit the models from the previous exercises again on the preprocessed training data.
4. Compute the RMSE again using the preprocessed testing data. Is the same model as last time still the best-fitting model?
5. Use the best-fitting model (based on RMSE) and predict `Sales` using three possible ways to split $6000 for the promotions budget. As with the training and testing data, be sure to preprocess the new data. What should the client do with their promotional budget? Provide an interpretation using dollars.
6. Render the Quarto document into Word and upload to Canvas.
